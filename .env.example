NODE_ENV=production
PORT=8080

# Google Gemini Developer API — keep the real value only in the deployment dashboard.
GEMINI_API_KEY=replace_in_deployment_environment_only
GEMINI_MODEL=gemini-2.5-flash

# Agentic OS identity
SIOS_OWNER_GID=399152573423

# Comma-separated browser origins allowed to call this API
ALLOWED_ORIGINS=https://siaas.space,https://www.siaas.space,http://localhost:5173

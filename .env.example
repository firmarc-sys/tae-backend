NODE_ENV=production
PORT=10000

# Google AI / Gemini — set the real key only in the deployment dashboard.
GOOGLE_API_KEY=replace_in_deployment_environment_only
GOOGLE_MODEL=gemini-2.5-flash

# Agentic OS identity
SIOS_OWNER_GID=399152573423

# Comma-separated browser origins allowed to call this API
ALLOWED_ORIGINS=https://siaas.space,https://www.siaas.space,http://localhost:5173
